version: "3"

services:
  app:
    build: .
    volumes:
      - ./app:/app
    ports:
      - "5000:5000"

  nginx:
    image: nginx
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
